services:
  db:
    image: docker.io/library/postgres:18-alpine
    container_name: studio_db_central
    environment:
      - POSTGRES_USER=studio_root
      - POSTGRES_PASSWORD=master_pass
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data:Z
