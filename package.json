{
  "name": "@jobchaser",
  "private": true,
  "workspaces": [
    "web",
    "api",
    "shared"
  ],
  "scripts": {
    "dev:web": "pnpm run dev -w web",
    "dev:api": "pnpm run dev -w api",
    "dev": "pnpm run dev --workspaces --if-present"
  }
}
