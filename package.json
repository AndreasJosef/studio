{
  "name": "field-logic-studio",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules",
    "dev": "bun run --filter '*' dev"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "bun-types": "latest",
    "typescript": "^5.0.0"
  }
}
